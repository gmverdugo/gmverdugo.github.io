---
/**
 * Mono code block matching mockup `pre.code` styling. Accepts
 * pre-tokenized HTML via the default slot so callers can mark
 * keyword/string/comment spans (.k/.s/.c) same as the mockup.
 */
---

<pre class="code"><slot /></pre>
